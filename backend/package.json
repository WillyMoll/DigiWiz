{
  "scripts": {
    "start": "ts-node src/index.ts",
    "deploy": "cd .. && git push heroku-backend `git subtree split --prefix backend master`:master --force"
  },
  "dependencies": {
    "@types/node": "^16.11.4",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "ts-node": "^10.4.0",
    "typescript": "^4.4.4"
  }
}
